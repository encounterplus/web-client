import { CanvasContainerDirective } from './canvas-container.directive';

describe('CanvasContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasContainerDirective();
    expect(directive).toBeTruthy();
  });
});
